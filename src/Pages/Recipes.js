import React from 'react';
import Menu from '../Menu';
const Recipes = (props) => {
    return (
        <div>
            <Menu/>
            <h1>Recipes</h1>
        </div>
    );
};

export default Recipes;